services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: redis-stack
    ports:
      - "6379:6379"
      - "8001:8765"
    environment:
      - REDIS_ARGS=--requirepass RedisPass032
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
